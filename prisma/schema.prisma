generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model count {
  id  Int @id(map: "pk_count") @default(autoincrement())
  sum Int @default(0)
}


